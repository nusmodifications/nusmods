# Set-up

(If we are reimplementing SAML in Rust, with the `samael` crate)
Build deps required for SAML auth:

```bash
$ sudo apt-get update
$ sudo apt-get install pkg-config libxml2-dev libclang-dev openssl libssl-dev xmlsec1 libtool libxml2 libxmlsec1-dev
```
