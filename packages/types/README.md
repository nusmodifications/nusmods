# @nusmods/types

Shared domain types and contracts for NUSMods packages.

This package is intended to hold cross-package data model types only. App shell,
Redux, router, browser, and vendor-specific declaration files should stay with
their owning package.
