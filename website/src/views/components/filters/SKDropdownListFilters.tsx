import React from 'react';
import { ListProps } from 'searchkit';
import styles from './styles.scss';

const DropdownListFiltersComponent = ({
  items,
  itemComponent,
  selectedItems,
  toggleItem,
  showCount,
  translate,
}: ListProps) => {
  console.log('9konen', items, selectedItems, showCount);
  return null;
};

export default DropdownListFiltersComponent;
