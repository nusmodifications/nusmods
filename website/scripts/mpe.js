const axios = require('axios');


const SSO_URL = "/api/auth/sso"
const MPE_URL = "/api/submissions"

// Get Link to Redirect user to NUS Login Page
function getAuthenticationURL() {
    
}

// Get MPE Preference
    // Catch 404 not found
    // Return empty array

    // else
    // Strip rank and only include module + module type

// Update MPE Preference
    // takes in an array of preferences and put to server

    // JSON return a message -> errors can be found here throw exact message